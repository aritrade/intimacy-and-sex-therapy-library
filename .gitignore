node_modules
.next
.env
.env.local
.env*.local

# Per-machine daily-spend ledger for lib/social/avatar.ts cap enforcement.
.replicate-usage.json
# Local-only working area (portrait candidates etc.)
.tmp/
*.log
.DS_Store
.vercel
.turbo
dist
coverage
.pnpm-store
eval-reports/
public/renders/

# TypeScript incremental build cache
*.tsbuildinfo

# Short-form video factory render output (can be large)
social/exports/
out/

# SadTalker local sandbox (only used if an operator runs the GH-Actions
# runner script locally for debugging — the workflow itself clones into
# a fresh runner each time).
sadtalker-src/
sadtalker-checkpoints/

# Local-only operator scripts + .env backups that may carry secrets.
scripts/_oneoff/
scripts/push-vercel-secrets.sh
.env*.bak
.env.check*

# Local agent / IDE scratch
.cursor/
.idea/
.vscode/

# Playwright + vitest artifacts
/test-results/
/playwright-report/
/playwright/.cache/
/coverage/

# Lighthouse CI
/.lighthouseci/
